let hangmanManager = {

};


const DEV_MODE = true;

if (DEV_MODE) {
    // Refresh When Page Clicked -- DEVELOPMENT --
    window.addEventListener("click", (e) => {
        window.location.reload();
    });
}
